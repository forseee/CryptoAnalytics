"use client";

import { Container } from "uiKit";

export default function Loading() {
  // Or a custom loading skeleton component
  return (
    <Container>
      <p>Loading</p>
    </Container>
  );
}
